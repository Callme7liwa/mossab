# Example environment variables for production deployment
# Copy this to .env and fill in your actual values

# Server configuration
PORT=3001

# Bridge API credentials
BRIDGE_API_TOKEN=your_bridge_api_token_here

# Database (optional - defaults to SQLite in same directory)
# DATABASE_PATH=/path/to/database.db

# Add other production secrets here as needed